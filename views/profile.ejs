<!doctype html>
<html>

<head>
  <title></title>
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
  <link rel="stylesheet" href="style.css">
</head>

<body>

  <div id="header">
    <nav id="nav">
      <img src="http://placekitten.com/g/100/100">
      <p href="/mission">Project Mission</p>
      <p>Your profile</p>
      <div class="login">
        <img src="" alt="">

        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
        </form>

      </div>
  </div>



  <!-- LOCAL INFORMATION -->
  <div class="well">
    <!-- <h3><span class="fa fa-user"></span> Local</h3>

        <% if (user.local.email) { %>
          <p>
            <strong>id</strong>: <%= user._id %><br>
              <strong>email</strong>: <%= user.local.email %><br>
                <strong>password</strong>: <%= user.local.password %>
          </p> -->

    <a href="/unlink/local" class="btn btn-default">Unlink</a>
    <% } else { %>
      <a href="/connect/local" class="btn btn-default">Connect Local</a>
      <% } %>
  </div>
  <div id="info">
    <h3>Your saved items</h3>
    <ul class="userSaved">
      <% for(var i=0; i<userSavedMigrations.length; i++) {%>
        <li class="message">
          <span><a href="<%= userSavedMigrations[i].article%>">
              <%= userSavedMigrations[i].title %>
            </a></span>
          <span>
            <%= userSavedMigrations[i].origins %>
          </span>
          <!-- <span><i class="fa fa-thumbs-up" aria-hidden="true"></i></span>
            <span><i class="fa fa-thumbs-down" aria-hidden="true"></i></span> -->
          <!--<span><i data-id="<%= userSavedMigrations[i]._id%>" class="fa fa-trash" aria-hidden="true"></i></span> -->
          <span>
            <<button data-id="<%= userSavedMigrations[i]._id%>" class="fa fa-trash" aria-hidden="true">
              DELETE</button>
          </span>
        </li>
        <% } %>
    </ul>

    <h2>Add a message</h2>
    <% if (user.local.email) { %>
      <form action="/messages" method="POST">
        <input type="text" value="<%= user.local.email %>" name="name">
        <input type="text" placeholder="message" name="msg">
        <button type="submit">Submit</button>
      </form>
  </div>
  <% } %>

    <script src="buttons.js"></script>
</body>

</html>